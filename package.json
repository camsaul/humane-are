{
  "name": "humane-are-2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "clean": "rimraf public/js",
    "test": "yarn && shadow-cljs compile test && node target/node-tests.js"
  },
  "devDependencies": {
    "rimraf": "~3.0.2",
    "shadow-cljs": "~2.10.13"
  }
}
